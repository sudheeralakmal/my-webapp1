version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  build:
    commands:
      - npm run build
    finally:
      - echo 'This is the finally block execution!'
artifacts:
  files:
    - "**/*"
  base-directory: "dist*"
  discard-paths: yes
